# Birthday nofitication system

Allows a user to add emails and DOBs of customers to a database and the system automatically sends out notifications on their birthdays

### How to run

- Install all packages.
- Create environment variables in a .env file using the .env.sample template
- Run project in root folder with `npm run dev` .
